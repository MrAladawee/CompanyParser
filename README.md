# CompanyParser — Парсер IT-компаний с обогащением через DaData

# 📌 О проекте
Проект представляет собой полностью автоматизированный пайплайн по сбору данных об IT-компаниях России из открытого интернет-источника, фильтрации по ОКВЭД, объединению с дополнительными сведениями и последующему обогащению данных через API DaData.  
Основная цель - получить сводный список компаний с ИНН, названием, адресом, численностью сотрудников и руководителями **используя только бесплатные источники**.

# Отчёт

---

## 1. Подход

### Источники данных
Первичные данные собирались с сайта **vypiska-nalog.com**:
- Сначала - перечень регионов и городов  
- Затем - список ОКВЭД для каждого города  
- Далее - карточки компаний, относящихся к ОКВЭД **62** и **63** (информационные технологии и деятельность информационных служб)  

Дополнительные сведения по численности сотрудников брались из локального файла `companyinfo.xlsx`, полученного путем использования сайта **rmsp.nalog.ru/search.html?mode=inn-list#** вручную. Были введены списком необходимые ИНН компаний, получены сведения в формате .xlsx (в перспективе можно было бы автоматизировать).  
Финальное обогащение выполнялось через сервис **DaData** (адрес + первое лицо).

---

### Логика отбора IT-компаний
Для отбора использовались следующие признаки:

1. ОКВЭД начинается с **62** или **63** — это наиболее точный маркер того, что компания относится к ИТ-сектору.  
2. Компания имеет статус **«Действующая»**.  
3. Удалялись дубликаты по ИНН.  
4. Дополнительно применялся фильтр по численности сотрудников ≥ 100 (на основе companyinfo.xlsx) — это позволило выделить устойчивые компании среднего и крупного размера.

---

### Оценка количества сотрудников
Количество сотрудников бралось из:
- столбца *«Среднесписочная численность работников за предшествующий календарный год»* в файле `companyinfo.xlsx`
- отсутствующие значения приводились к 0
- итоговая метрика использовалась для формирования двух датасетов:
  - `companies_noFilter.csv` - все компании (если необходим полный список)  
  - `companies.csv` - отфильтрованные по численности ≥ 100, т.е. итоговый файл

---

## 2. Что получилось / что не получилось

### Что удалось реализовать
- Полностью автоматизированный пайплайн данных от сырого HTML-источника до итогового CSV.  
- Корректный парсинг:
  - регионов и городов  
  - ОКВЭД 62/63  
  - карточек компаний  
  - адресов компаний  
- Объединение данных с Excel-файлом по ИНН.  
- Обогащение информации (адрес, руководитель) через **DaData**.  
- Структура Python-проекта (`src/`, модули, main.py).  
- Защищённая работа с секретом API через `.env`.  

---

### Что не успели / не реализовано (но планировалось)
1. Автоматический параллельный парсинг с использованием `asyncio` / `aiohttp` для ускорения работы (текущее примерное время выполнения сбора данных по получению всех компаний - 2 часа).  
2. Использование Selenium под источники с JS логикой для получения данных по доходам/расходам компаний (подход был почти успешно реализованн, но ловил постоянно капчу).  
3. Получение краткой информации/сайта компании (идейно можно было обращаться к поиску через hh.ru для получения карточки компании и последующей обработкой LLM для суммаризации описания в 1-2 предложения) 

---

## 3. Роль LLM

В работе использовались LLM (ChatGPT):
  
- для проверки HTML-структуры и формирования корректных CSS-селекторов;
- для предложений по обработке данных и улучшению архитектуры пайплайна;

---

# Инструкция запуска кода

## 1. Клонировние

```python
git clone https://github.com/MrAladawee/CompanyParser.git
cd CompanyParser
```

## 2. Создание и активация виртуального окружения

```Windows
python -m venv .venv
.venv\Scripts\activate
```

```Linux/macOS
python -m venv .venv
.venv\Scripts\activate
```

## 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

## 4. Настройка переменных окружения

Создайте файл .env в корне проекта и поместите туда токен DaData:

```ini
DADATA_TOKEN=токен
```

## 5. Запуск

### 5.1. Этап 1 - сбор основных данных

```bash
python -m src.main collect
```

После выполнения появятся:

data/regions_and_cities.csv
data/it_companies_okved_62_63.csv

Далее необходимо вручную выполнить следующие действия:

1. Открыть файл data/it_companies_okved_62_63.csv
2. Взять список ИНН
3. Загрузить на сайт **https://rmsp.nalog.ru/search.html?mode=inn-list#**
4. Скачать Excel-файл с информацией
5. Сохранить в папку data/companyinfo.xlsx
6. Можно перейти к этапу 2

### 5.2. Этап 2 - объединение данных + обогащение через DaData

```bash
python -m src.main process
```

На выходе получится файл data/companies.csv - необходимый результат

## 6. Для повторного запуска удалить промежуточные файлы:

regions_and_cities.csv
it_companies_okved_62_63.csv
companies_noFilter.csv
companies.csv

и повторное выполнение шагов 5.1 и 5.2.

## 7. Завершение работы

```bash
deactivate
```
